public class EqualSumChecker {
    public static boolean EqualSumChecker(int numOne, int numTwo, int numThree){

        if(numOne + numTwo == numThree) {
            return true;
        } return false;
    }
}
